import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    root: {
        margin: 10,
        padding: 10,
        borderWidth: 1
    },
    buttonText: {
        fontSize: 20,
        fontFamily: 'NotoSerif-Bold'
    }
});
