import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    root: {
        margin: 10,
        paddingHorizontal: 32,
        paddingVertical: 16,
        borderWidth: 1,
        display: 'flex',
        flexDirection: 'row',
        justifyContent: 'center',
        alignItems: 'center',
        backgroundColor: '#c1c1c1',
        // width: 220,
        borderRadius: 10
    },
    buttonText: {
        fontSize: 20,
        fontFamily: 'NotoSerif-Bold',
        marginRight: 16
    }
});

// hello, let me test if it's working fine or not
